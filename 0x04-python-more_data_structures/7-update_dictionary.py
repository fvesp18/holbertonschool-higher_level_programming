#!/usr/bin/python3


def update_dictionary(a_dictionary, key, value):
    if a_dictionary:
        a_dictionary[key] = a_dictionary.update(a_dictionary[value])
    else:
        a_dictionaru[key] = value
